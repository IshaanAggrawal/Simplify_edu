{
  "id": "sliding-window",
  "name": "Sliding Window Maximum",
  "category": "Arrays",
  "pattern": "Sliding Window",
  "difficulty": "Hard",
  "leetcode_problems": [239],
  "languages": {
    "cpp": "vector<int> maxSlidingWindow(vector<int>& nums, int k) {\n    deque<int> dq;\n    vector<int> res;\n    for (int i = 0; i < nums.size(); i++) {\n        if (!dq.empty() && dq.front() == i - k) dq.pop_front();\n        while (!dq.empty() && nums[dq.back()] < nums[i]) dq.pop_back();\n        dq.push_back(i);\n        if (i >= k - 1) res.push_back(nums[dq.front()]);\n    }\n    return res;\n}"
  },
  "default_input": { "nums": [1,3,-1,-3,5,3,6,7], "k": 3 },
  "pattern_explanation": "Maintain a window of size k. Use a deque to store indices of potential maximums.",
  "when_to_use": "When dealing with contiguous subarrays or subsets of a specific size.",
  "time_complexity": "O(n)",
  "space_complexity": "O(k)",
  "steps": [
    {
      "step": 0,
      "description": "Initialize window boundaries.",
      "code_line": "deque<int> dq;",
      "variables": { "i": 0, "k": 3 },
      "nodes": [1,3,-1,-3,5,3,6,7],
      "edges": [],
      "pointers": { "left": 1, "right": 3 },
      "highlighted_nodes": [1, 3, -1],
      "reversed_edges": []
    }
  ]
}
